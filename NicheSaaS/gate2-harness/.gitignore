# Generated by fixtures/make_fixtures.py — deterministic, so regenerate rather
# than commit the images.
fixtures/inspections/

# Pipeline output, including the library fingerprint from the last run.
out/

# The procedure library is authored content we own — it belongs in version
# control, where it gets history and review. It is the asset, not a liability.
#
# What must never land here: customer inspection data, and any licensed
# standards text. If you ever convert a purchased standard to text for
# reference, keep it outside the working tree entirely.
raw/
*-licensed.md
*-licensed.txt

.venv*/
__pycache__/
*.pyc
